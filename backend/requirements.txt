# ==========================================================
# Barkodcu Cepte — Production Requirements (Railway)
# Sadece kodda gerçekten import edilen paketler listelendi.
# Dev tooling (black, flake8, mypy, pytest) çıkarıldı.
# emergentintegrations kaldırıldı (kullanılmıyor, public PyPI'da yok).
# ==========================================================

# FastAPI + ASGI
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
python-multipart==0.0.22

# Pydantic + validation
pydantic==2.12.5
pydantic_core==2.41.5
email-validator==2.3.0

# HTTP client (POS sync.php çağrıları için)
httpx==0.28.1
aiohttp==3.13.3
requests==2.32.5

# Database drivers
motor==3.3.1
pymongo==4.5.0
aiomysql==0.3.2
PyMySQL==1.1.2

# Auth (JWT + password)
python-jose[cryptography]==3.5.0
bcrypt==4.1.3
passlib[bcrypt]==1.7.4
PyJWT==2.11.0

# Env + utils
python-dotenv==1.2.1
dnspython==2.8.0
